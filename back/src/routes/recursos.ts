import { Router, Request, Response } from 'express';

interface IRecursos {
    id: number;
    nombre: string;
}

export default IRecursos;
//esta interfaz nos permite realizar validaciones dentro de las rutas que haremos en su momento

 
